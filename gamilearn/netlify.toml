# Single Page App fallback: serve index.html for all routes (200), enabling client-side routing
# Note: force = false allows Netlify to serve static files (JS, CSS, images) first
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false